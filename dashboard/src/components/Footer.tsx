export default function Footer() {
  return (
    <footer className="border-t border-wri-line py-8 text-sm text-wri-muted">
      <p>
        Diagnóstico territorial urbanístico — visualização gerada a partir do GeoPackage do
        projeto. Para a análise geoespacial completa, abra o <code>.gpkg</code> no QGIS.
      </p>
    </footer>
  );
}
